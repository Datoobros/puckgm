// 2026-27 is the season this first pass of matchups targets (per explicit
// product direction — the real upcoming season, even though scores read
// 0-0 until it actually starts). 2026-09-29 is the real NHL schedule's
// first regular-season date, confirmed against the live schedule API.
// Revisit this pair of constants once matchup periods stop being a
// single-season, commissioner-triggered thing.
export const CURRENT_SCHEDULE_SEASON = 2026;
export const DEFAULT_SEASON_START = "2026-09-29";
